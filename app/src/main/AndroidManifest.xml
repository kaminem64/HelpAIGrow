<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.google.cloud.android.speech">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_scheme"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".WelcomeActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ConsentFormActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".AuthActivity"
            android:autoLink="web"
            android:screenOrientation="portrait" />
        <activity
            android:name=".QuestionsActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ManualCommandsActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ExpOneSpeechActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ExpTwoSpeechActivity"
            android:screenOrientation="portrait" />
        <service android:name=".SpeechService" />
        <service android:name=".ResponseService" />
    </application>

</manifest>